/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type WritableAccount,
} from '@solana/web3.js';
import { PALADIN_STAKE_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const VALIDATOR_SYNC_AUTHORITY_DISCRIMINATOR = 15;

export function getValidatorSyncAuthorityDiscriminatorBytes() {
  return getU8Encoder().encode(VALIDATOR_SYNC_AUTHORITY_DISCRIMINATOR);
}

export type ValidatorSyncAuthorityInstruction<
  TProgram extends string = typeof PALADIN_STAKE_PROGRAM_PROGRAM_ADDRESS,
  TAccountConfig extends string | IAccountMeta<string> = string,
  TAccountValidatorStake extends string | IAccountMeta<string> = string,
  TAccountValidatorVote extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountValidatorStake extends string
        ? WritableAccount<TAccountValidatorStake>
        : TAccountValidatorStake,
      TAccountValidatorVote extends string
        ? WritableAccount<TAccountValidatorVote>
        : TAccountValidatorVote,
      ...TRemainingAccounts,
    ]
  >;

export type ValidatorSyncAuthorityInstructionData = { discriminator: number };

export type ValidatorSyncAuthorityInstructionDataArgs = {};

export function getValidatorSyncAuthorityInstructionDataEncoder(): Encoder<ValidatorSyncAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: VALIDATOR_SYNC_AUTHORITY_DISCRIMINATOR,
    })
  );
}

export function getValidatorSyncAuthorityInstructionDataDecoder(): Decoder<ValidatorSyncAuthorityInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getValidatorSyncAuthorityInstructionDataCodec(): Codec<
  ValidatorSyncAuthorityInstructionDataArgs,
  ValidatorSyncAuthorityInstructionData
> {
  return combineCodec(
    getValidatorSyncAuthorityInstructionDataEncoder(),
    getValidatorSyncAuthorityInstructionDataDecoder()
  );
}

export type ValidatorSyncAuthorityInput<
  TAccountConfig extends string = string,
  TAccountValidatorStake extends string = string,
  TAccountValidatorVote extends string = string,
> = {
  /** Config */
  config: Address<TAccountConfig>;
  /** Validator stake */
  validatorStake: Address<TAccountValidatorStake>;
  /** Validator vote */
  validatorVote: Address<TAccountValidatorVote>;
};

export function getValidatorSyncAuthorityInstruction<
  TAccountConfig extends string,
  TAccountValidatorStake extends string,
  TAccountValidatorVote extends string,
>(
  input: ValidatorSyncAuthorityInput<
    TAccountConfig,
    TAccountValidatorStake,
    TAccountValidatorVote
  >
): ValidatorSyncAuthorityInstruction<
  typeof PALADIN_STAKE_PROGRAM_PROGRAM_ADDRESS,
  TAccountConfig,
  TAccountValidatorStake,
  TAccountValidatorVote
> {
  // Program address.
  const programAddress = PALADIN_STAKE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    validatorStake: { value: input.validatorStake ?? null, isWritable: true },
    validatorVote: { value: input.validatorVote ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.validatorStake),
      getAccountMeta(accounts.validatorVote),
    ],
    programAddress,
    data: getValidatorSyncAuthorityInstructionDataEncoder().encode({}),
  } as ValidatorSyncAuthorityInstruction<
    typeof PALADIN_STAKE_PROGRAM_PROGRAM_ADDRESS,
    TAccountConfig,
    TAccountValidatorStake,
    TAccountValidatorVote
  >;

  return instruction;
}

export type ParsedValidatorSyncAuthorityInstruction<
  TProgram extends string = typeof PALADIN_STAKE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Config */
    config: TAccountMetas[0];
    /** Validator stake */
    validatorStake: TAccountMetas[1];
    /** Validator vote */
    validatorVote: TAccountMetas[2];
  };
  data: ValidatorSyncAuthorityInstructionData;
};

export function parseValidatorSyncAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedValidatorSyncAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      validatorStake: getNextAccount(),
      validatorVote: getNextAccount(),
    },
    data: getValidatorSyncAuthorityInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
